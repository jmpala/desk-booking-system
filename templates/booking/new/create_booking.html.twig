{% extends '_templates/base.html.twig' %}
{% set module = 'bookings' %}
{% set date = app.request.query.get('date')|default('now'|date('d-m-Y')) %}

{% block title %}New Booking{% endblock %}

{% block main_header %}New Booking{% endblock %}

{% block body %}

    {% include "_parts/breadcrumbs/_create_booking_bread_crumb.html.twig" with { 'editStage': '0' } %}

    {% include "_parts/forms/_input_create_booking_form.html.twig" with {
        'action': path('app_booking_new_confirm'),
        'hasUserId': false,
        'hasErrors': true,
        'hasCsrf': true,
        'csrfName': '_csrf_token',
        'csrfValue': csrf_token('newBooking'),
        'allBookables': allBookables,
        'startingDate': date,
        'returnTo': 'window.history.back();',
        'returnLbl': 'Return',
        'submitLbl': 'Create',
    } %}

{% endblock %}
