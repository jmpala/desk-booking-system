{% extends '_templates/base.html.twig' %}
{% set module = 'planning' %}

{% block title %}New Booking{% endblock %}

{% block body %}

    <h1 class="mt-4 mb-3 text-center">New Booking for {{ selectedUser.userIdentifier }}</h1>

    {% include "booking/new/_parts/_createBookingBreadCrumb.html.twig" with { 'editStage': '1' } %}

    {% include "_parts/forms/_confirmCreateBookingForm.html.twig" with {
        'action': path('app_planning_processnewbookingandshowconfirmation'),
        'hasUserId': true,
        "hasCsrf": true,
        'csrfName': '_csrf_token',
        'csrfValue': csrf_token('newPlanningBooking'),
        'bookable': bookable,
        'fromDate': fromDate,
        'toDate': toDate,
        'returnTo': 'window.history.back();',
        'returnLbl': 'Modify',
        'submitLbl': 'Confirm'
    } %}

{% endblock %}
