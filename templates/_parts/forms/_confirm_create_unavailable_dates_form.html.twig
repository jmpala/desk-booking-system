<form class="form border border-primary bg-light rounded p-3" action="{{ action }}" method="post">

    {% if hasCsrf %}
        <input type="hidden" name="{{ csrfName }}" value="{{ csrfValue }}">
    {% endif %}

    <div class="form-floating m-3">
        <select class="form-select" id="allBookables" disabled>
            <option value="{{ bookable.id }}"> {{ bookable.code }}</option>
        </select>
        <input type="hidden" name="bookable" value="{{ bookable.id }}">
        <label for="allBookables">Available Bookables</label>
    </div>

    <div class="form-floating m-3">
        <input class="form-control" type="date" id="fromDate" value="{{ fromDate | date('Y-m-d') }}" disabled>
        <input type="hidden" name="fromDate" value="{{ fromDate | date('Y-m-d') }}">
        <label for="fromDate">From Date</label>
    </div>

    <div class="form-floating m-3">
        <input class="form-control" type="date" name="toDate" id="toDate" value="{{ toDate | date('Y-m-d') }}" disabled>
        <input type="hidden" name="toDate" value="{{ toDate | date('Y-m-d') }}">
        <label for="toDate">To Date</label>
    </div>

    <div class="form-floating m-3">
        <textarea class="form-control" placeholder="Enter notes..." id="notes" disabled>{{ notes }}</textarea>
        <input type="hidden" name="notes" value="{{ notes }}">
        <label for="toDate">Notes</label>
    </div>

    <div class="row justify-content-center m-3">
        <button type="button" class="btn btn-outline-primary btn-lg col-2 m-2" onclick="{{ returnTo }}">{{ returnLbl }}</button>
        <button type="submit" class="btn btn-primary btn-lg col-2 bg-primary m-2" id="submitBtn">{{ submitLbl }}</button>
    </div>

</form>